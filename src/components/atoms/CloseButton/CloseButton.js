import React from 'react';
import { Wrapper, CloseRight, CloseLeft } from './CloseButton.styles';

const CloseButton = (props) => {
  return (
    <Wrapper {...props}>
      <div />
      <div />
    </Wrapper>
  );
};

export default CloseButton;
